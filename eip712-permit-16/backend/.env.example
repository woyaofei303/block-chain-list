PGHOST=127.0.0.1
PGPORT=5432
PGDATABASE=tokenbank_permit_16
RPC_URL=http://127.0.0.1:8547
CHAIN_ID=31337
# 必须填写实际 Token 合约地址；不要填写银行地址。
TOKEN_ADDRESS=
# 使用 Token 实际部署区块。
START_BLOCK=0
# 0 仅用于本地按交易出块的 Anvil；公共网络另行配置确认数。
CONFIRMATIONS=0
BATCH_SIZE=100
POLL_INTERVAL_MS=1000
HOST=127.0.0.1
PORT=13016
# 填写新部署的 IdempotentTokenBank 才启用 SIWE 和操作写接口；留空保持只读索引。
BANK_ADDRESS=
# 必须与浏览器实际 origin 完全一致，包括协议、主机、端口；线上使用 HTTPS。
PUBLIC_ORIGIN=http://127.0.0.1:3016
